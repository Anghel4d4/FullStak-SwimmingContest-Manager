package main.swimmingnetworking;

public enum RequestType {
    LOGIN,
    LOGOUT,
    GET_PROBES,
    FIND_PARTICIPANTS_BY_PROBA,
    REGISTER_PARTICIPANT,
    SAVE_PARTICIPANT,
    FIND_NAME_AND_PRENUME

}
